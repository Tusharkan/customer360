name: "customer360"
version: "1.0.0"
config-version: 2

profile: "customer360"

model-paths: ["models"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  customer360:
    staging:
      +materialized: view

    marts:
      +materialized: table